import React from 'react';

function Minutely(props) {
    const myObj = props.data
    if (myObj !== undefined) {
        
    }

    return (
        <div>
            Minutely
            {/* {console.log(myObj)} */}
        </div>
    );
}

export default Minutely;