import React from 'react';

function Daily(props) {
    const myObj = props.data
    if (myObj !== undefined) {
        
    }

    return (
        <div>
            Daily
            {/* {console.log(myObj)} */}
        </div>
    );
}

export default Daily;