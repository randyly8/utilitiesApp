import React from 'react';

function Hourly(props) {
    const myObj = props.data
    if (myObj !== undefined) {
        
    }

    return (
        <div>
            Hourly
            {/* {console.log(myObj)} */}
        </div>
    );
}

export default Hourly;